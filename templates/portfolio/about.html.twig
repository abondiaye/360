{% extends 'base.html.twig' %}

{% block title %}À propos | GeniusPro360°{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            background: linear-gradient(120deg, #0f172a, #1e293b);
            color: #f1f5f9;
            overflow-x: hidden;
            position: relative;
            font-family: 'Poppins', sans-serif;
        }

        .glow-bg {
            position: fixed;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at center, rgba(250, 204, 21, 0.1) 0%, transparent 70%);
            animation: pulse 8s infinite;
            z-index: -1;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                opacity: 0.6;
            }
            50% {
                transform: scale(1.2);
                opacity: 0.2;
            }
        }

        .about-hero {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
            padding: 4rem 2rem;
            animation: fadeIn 1.5s ease-in-out;
        }

        .about-hero img {
            border-radius: 50%;
            width: 160px;
            height: 160px;
            box-shadow: 0 0 40px rgba(250, 204, 21, 0.6);
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            0% { box-shadow: 0 0 10px rgba(250, 204, 21, 0.3); }
            100% { box-shadow: 0 0 30px rgba(250, 204, 21, 0.8); }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .about-section {
            padding: 4rem 2rem;
            background-color: rgba(255, 255, 255, 0.02);
        }

        .about-section h2 {
            color: #facc15;
            font-size: 1.8rem;
            margin-top: 2rem;
            position: relative;
            display: inline-block;
            text-shadow: 0 0 10px #facc15, 0 0 20px #facc15;
        }

        .about-section h2::after {
            content: "";
            position: absolute;
            width: 50%;
            height: 2px;
            background: #facc15;
            left: 0;
            bottom: -8px;
        }

        .btn-contact {
            background: linear-gradient(90deg, #facc15, #fde68a);
            color: #1e293b;
            font-weight: bold;
            padding: 12px 24px;
            border-radius: 12px;
            text-decoration: none;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 0 15px rgba(250, 204, 21, 0.5);
        }

        .btn-contact:hover {
            background: linear-gradient(90deg, #fde68a, #facc15);
            box-shadow: 0 0 25px rgba(250, 204, 21, 0.8);
            transform: scale(1.05);
        }
    </style>
{% endblock %}

{% block body %}
<div class="glow-bg"></div>

<section class="about-hero">
    <img src="{{ asset('images/profile.png') }}" alt="Abdoulaye N'diaye - GeniusPro360°">
    <h1 class="display-5 fw-bold">Hello, moi c’est Abdoulaye 👋</h1>
    <p class="lead mt-3">Freelance IT | Technicien Réseau & Développeur Web Symfony / Vue.js</p>
</section>

<section class="about-section">
    <div class="container max-w-4xl mx-auto space-y-5">

        <p data-aos="fade-up">
            Passionné par la technologie depuis toujours, j’ai décidé de combiner mes deux forces :
            <strong>le support informatique</strong> et le <strong>développement web</strong>.
            Mon but ? Offrir à mes clients des solutions fiables, performantes et humaines.
        </p>

        <div data-aos="fade-right">
            <h2 class="h4 fw-bold">🛠 Technicien Réseau</h2>
            <p>
                J’interviens sur site ou à distance pour configurer vos équipements : routeurs, switchs, serveurs, NAS, sécurité, etc.
                Je garantis stabilité, sécurité et accompagnement personnalisé.
            </p>
        </div>

        <div data-aos="fade-left">
            <h2 class="h4 fw-bold">💻 Développeur Web</h2>
            <p>
                J’utilise Symfony, Vue.js, SCSS pour créer des sites et plateformes intuitives, rapides et bien sécurisées.
                J’intègre des dashboards, des API REST, ou même des outils de diagnostic automatisés.
            </p>
        </div>

        <div data-aos="fade-up">
            <h2 class="h4 fw-bold">🔐 CyberOps & Sécurité</h2>
            <p>
                Formé aux pratiques de cybersécurité, j’intègre dès le départ les protections nécessaires :
                rôles, filtrage, validation, anti-injection, audit de code.
            </p>
        </div>

        <div data-aos="fade-up">
            <h2 class="h4 fw-bold">🚀 Mon ADN</h2>
            <ul class="list-unstyled ms-4">
                <li>✔ Écoute active et compréhension client</li>
                <li>✔ Réactivité (SLA rapide sur demande)</li>
                <li>✔ Curiosité & apprentissage constant</li>
                <li>✔ Éthique, confidentialité, confiance</li>
            </ul>
        </div>

        <div class="mt-4 text-center" data-aos="fade-up">
            <a href="{{ path('app_register') }}">S'inscrire</a> 

            <a href="{{ path('contact') }}" class="btn-contact">📬 Me contacter</a>
        </div>

    </div>
</section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
    <script>
        AOS.init({
            duration: 800,
            once: true
        });
    </script>
{% endblock %}
