<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GeniusPro 360° - Assistance</title>
    <link rel="icon" href="{{ asset('images/logo.png') }}" type="image/x-icon">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <style>
        /* Fond d'écran avec Vanta */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
}

        #vanta-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        /* Conteneur principal */
        .container {
            position: relative;
            z-index: 1;
            color: black;
            text-align: center;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.27); /* Fond légèrement transparent */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            max-width: 80%; /* Largeur maximale du conteneur */
            max-height: 80vh; /* Hauteur maximale du conteneur pour éviter qu'il soit trop haut */
            overflow-y: auto; /* Ajoute un défilement si le contenu dépasse la hauteur */
            display: flex;
            flex-direction: column;
            justify-content: center; /* Centre le contenu verticalement */
        }

        /* Style du texte du conteneur */
        .container h1 {
            font-size: 2.5em;
            font-weight: bold;
        }

        .container p {
            font-size: 1.5em;
            margin: 10px 0 20px;
        }

        /* Style du logo */
        .logo {
            max-width: 200px;
            width: 50%;
            margin-bottom: 20px;
        }

        /* Style des boutons */
        button {
            align-self: center;
            border: solid 0.625em transparent;
            padding: 22px;
            width: 150px;
            height: 85px;
            border-radius: 10px;
            font-size: 1.2em;
            color: white;
            text-shadow: 0 0 1px currentColor, 0 0 2px dimgrey;
            cursor: pointer;
            background: conic-gradient(#edc800, #e3b600, #f3cf00, #ffe800, #ffe900, #ffeb00, #ffe000, #ebc500, #e0b100, #f1cc00, #fcdc00, #ffe500, #fad900, #eec200, #e7b900, #f7d300, #ffe800, #ffe300, #f5d100, #e6b900, #e3b600, #f4d000, #ffe400, #ebc600, #e3b600, #f6d500, #ffe900, #ffe90a, #edc800);
            margin: 10px;
            transition: transform 0.3s, background-color 0.3s;
        }

        button:hover {
            transform: scale(1.05);
        }

        /* Styles des sections FAQ et contact */
        .faq, .contact-info {
            margin: 20px 0;
            text-align: left;
        }

        .faq-item {
            margin-bottom: 15px;
        }

        .faq-item h3 {
            cursor: pointer;
            margin: 0;
        }

        .faq-item p {
            display: none;
            margin: 5px 0 0;
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .contact-form input, .contact-form textarea {
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .contact-form button {
            background-color: #4CAF50; /* Vert */
            border: none;
            color: white;
        }

        /* Style pour le bouton de retour */
        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border-radius: 5px;
            text-decoration: none;
        }

        .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div id="vanta-bg"></div>

    <div class="container">
        <!-- Logo au centre -->
        <img src="{{ asset('images/logo.png') }}" alt="GeniusPro 360° Logo" class="logo">
        <h1>Bienvenue sur GeniusPro 360°</h1>
        <p>Une assistance informatique en temps réel avec réalité augmentée.</p>

       
        <!-- Boutons stylisés -->
        <button onclick="window.location.href='{{ path('app_video_call') }}'">Assistance en RA</button>
        <button onclick="window.location.href='{{ path('app_diagnostic') }}'">Diagnostic</button>
        <button onclick="window.location.href='{{ path('blog_index') }}'">Blog</button>

        <!-- Lien vers les articles de blog -->
        <a href="{{ path('blog_index') }}">Voir les articles de blog</a>

        <!-- Section FAQ -->
        <h2>Questions Fréquemment Posées (FAQ)</h2>
        <div class="faq">
            <div class="faq-item">
                <h3>Comment puis-je contacter le support ?</h3>
                <p>Vous pouvez nous contacter par email à <strong>support@example.com</strong> ou par téléphone au <strong>0123 456 789</strong>.</p>
            </div>
            <div class="faq-item">
                <h3>Quels types de problèmes peuvent être résolus ?</h3>
                <p>Nous aidons avec une variété de problèmes techniques, y compris des problèmes de connexion, de configuration, et d'utilisation de notre plateforme.</p>
            </div>
            <div class="faq-item">
                <h3>Quels sont les horaires de support ?</h3>
                <p>Notre équipe de support est disponible du lundi au vendredi, de 9h00 à 17h00.</p>
            </div>
        </div>

        <!-- Section Contact -->
        <h2>Contactez-nous</h2>
        <div class="contact-info">
            <p>Si vous avez besoin d'assistance, veuillez remplir le formulaire ci-dessous :</p>
            <form class="contact-form" action="{{ path('app_contact') }}" method="post">
                <input type="text" name="name" placeholder="Votre nom" required />
                <input type="email" name="email" placeholder="Votre email" required />
                <textarea name="message" placeholder="Votre message..." required></textarea>
                <button type="submit">Envoyer</button>
            </form>
        </div>
    </div>

    <!-- Scripts pour l'animation Vanta -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.fog.min.js"></script>
    <script>
        let vantaEffect;
        document.addEventListener("DOMContentLoaded", () => {
            vantaEffect = VANTA.FOG({
                el: "#vanta-bg",
                highlightColor: 0xffd700,
                midtoneColor: 0xe5c100,
                lowlightColor: 0xa67c00,
                baseColor: 0x1e1e1e,
                blurFactor: 0.6,
                speed: 2.0,
                zoom: 1.0
            });
        });

        // Toggle FAQ answers
        document.querySelectorAll('.faq-item h3').forEach(item => {
            item.addEventListener('click', () => {
                const content = item.nextElementSibling;
                content.style.display = content.style.display === 'none' || content.style.display === '' ? 'block' : 'none';
            });
        });
    </script>
</body>
</html>
