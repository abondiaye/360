<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GeniusPro 360°</title>
    <link rel="icon" href="{{ asset('images/logo.png') }}" type="image/x-icon">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <style>
        /* Fond d'écran avec Vanta */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }
        #vanta-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        /* Conteneur principal */
        .container {
            position: relative;
            z-index: 1;
            color: black;
            text-align: center;
        }

        /* Style du texte du conteneur en dégradé gris métallique */
        .container h1,
        .container p {
            font-size: 2em;
            font-weight: bold;
            background: linear-gradient(45deg, #D3D3D3, #A9A9A9, #808080, #696969, #4F4F4F);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Style du texte du conteneur en dégradé doré métallique */
        .container h1,
        .container p {
            font-size: 2em;
            font-weight: bold;
            background: linear-gradient(45deg, #FFD700, #E5C100, #A67C00, #8B7500, #5C4F00);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }


        /* Style du logo */
        .logo {
            max-width: 200px;
            width: 50%;
            margin-bottom: 20px;
        }

        /* Style des boutons */
        button {
            align-self: center;
            border: solid 0.625em transparent;
            padding: 3px;
            width: 6.25em;
            height: 6.25em;
            border-radius: 1.09375em;
            font: 2.5em/5em Satisfy, cursive;
            color: #eedc00;
            text-shadow: 0 0 1px currentColor, 0 0 2px dimgrey;
            cursor: pointer;
            background: conic-gradient(#edc800, #e3b600, #f3cf00, #ffe800, #ffe900, #ffeb00, #ffe000, #ebc500, #e0b100, #f1cc00, #fcdc00, #ffe500, #fad900, #eec200, #e7b900, #f7d300, #ffe800, #ffe300, #f5d100, #e6b900, #e3b600, #f4d000, #ffe400, #ebc600, #e3b600, #f6d500, #ffe900, #ffe90a, #edc800) content-box, linear-gradient(#f6d600, #f6d600) padding-box, radial-gradient(rgba(120, 120, 120, 0.9), rgba(120, 120, 120, 0) 70%) 50% bottom/80% 0.46875em no-repeat border-box;
            margin: 10px;
        }

        /* Style du texte en dégradé pour les boutons */
        button span {
            background: linear-gradient(45deg, #D3D3D3, #A9A9A9, #808080, #696969, #4F4F4F);            
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        button:nth-child(1) {
            box-shadow: inset 0 0 0 1px #eedc00, inset 0 1px 2px rgba(255, 255, 255, 0.5), inset 0 -1px 2px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>
    <div id="vanta-bg"></div>

    <div class="container">
        <!-- Logo au centre -->
        <img src="{{ asset('images/logo.png') }}" alt="GeniusPro 360° Logo" class="logo">
        <h1>Bienvenue sur GeniusPro 360°</h1>
        <p>Une assistance informatique en temps réel avec réalité augmentée.</p>

    <!-- Boutons stylisés autour du logo -->
        <button onclick="window.location.href='{{ path('app_video_call') }}'"><span>Assistance en RA</span></button>
        <button onclick="window.location.href='{{ path('app_diagnostic') }}'"><span>Diagnostic</span></button>
        <button onclick="window.location.href='{{ path('app_blog') }}'"><span>Blog</span></button>

    </div>

    <!-- Scripts pour l'animation Vanta -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/vanta@latest/dist/vanta.fog.min.js"></script>
    <script>
        let vantaEffect;
        document.addEventListener("DOMContentLoaded", () => {
            vantaEffect = VANTA.FOG({
                el: "#vanta-bg",
                highlightColor: 0xffd700,
                midtoneColor: 0xe5c100,
                lowlightColor: 0xa67c00,
                baseColor: 0x1e1e1e,
                blurFactor: 0.6,
                speed: 2.0,
                zoom: 1.0
            });
        });
    </script>
</body>
</html>
